#include <stdio.h>

int main(void) {
    const char *first = "alpha";
    const char *second = "beta";
    const char *third = "gamma";

    printf("banner:%s|%s|%s\n", first, second, third);
    return 0;
}
